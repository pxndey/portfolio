# Backend origin the Bun server proxies /api/audio, /api/music, /api/audio/file to.
# bun run dev uses DEV_ENV; production (bun start / docker) uses PROD_ENV.
PROD_ENV=http://backend:8080
DEV_ENV=http://localhost:8080
